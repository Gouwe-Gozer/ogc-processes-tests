{
  "id": "process_invalid_enum_saga_resample",
  "title": "Unadvertised SAGA enum value is accepted by the provider",
  "category": "validation",
  "execution": "manual-only",
  "process_id": "SAGA.grid_tools.0",
  "source": {
    "process_description": "../../../deployments/zoo-local/captures/descriptions/SAGA.grid_tools.0.process.json",
    "representative_response": "Captured from the local ZOO profile on 2026-08-25."
  },
  "validation": {
    "expected": "invalid",
    "violations": [
      {
        "path": "inputs.TARGET_DEFINITION",
        "rule": "enum: user defined or grid or grid system",
        "received": "not advertised"
      }
    ]
  },
  "client_value": [
    "Description-driven enum validation and exact value preservation.",
    "Server HTTP success does not expand the enum advertised to a generic client."
  ],
  "steps": [
    {
      "id": "execute-invalid-body",
      "request": "01-execute-invalid.request.json",
      "representative_response": "01-execute-invalid.response.json",
      "expect": {
        "client_preflight": "invalid",
        "observed_http_status_if_sent": [
          200
        ],
        "observed_result_ids_if_sent": [
          "OUTPUT"
        ]
      },
      "expected_client_behavior": {
        "classification": "description-validation-error",
        "must": [
          "List the two advertised TARGET_DEFINITION values and normally block submission.",
          "If explicitly sent as raw JSON, preserve the original value and parse the returned reference without changing the captured contract."
        ],
        "must_not": [
          "Silently replace the unknown value with the default.",
          "Conclude that not advertised became a supported enum member because this provider returned HTTP 200.",
          "Send this request from the ordinary description-driven form flow."
        ]
      }
    }
  ],
  "deployment": "zoo-local"
}
