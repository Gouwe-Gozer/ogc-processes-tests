{
  "id": "gdal_grid_points",
  "title": "Grid the five Alkmaar elevation points",
  "process_id": "Gdal_Grid",
  "execution_mode": "sync",
  "request": "request.json",
  "fixture": "../../fixtures/points/five_points_3d.geojson",
  "expected": {
    "http_status": 500
  },
  "notes": "Expected-error local-profile case. The advertised inputs reach the provider, but it returns 500 without a useful message because its implementation needs unadvertised layer/band parameters. The request intentionally does not invent them."
}
