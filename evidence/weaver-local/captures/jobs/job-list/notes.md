# Weaver job list

Weaver returns a JSON object with job IDs, paging information, filters, and
links. The captured list contains the test jobs created while collecting this
evidence; those IDs are not stable.
