# Missing required input

An empty execution body returned a structured JSON error naming the first
missing input and its required cardinality. This is useful validation feedback
that a client can pass through to the user.
