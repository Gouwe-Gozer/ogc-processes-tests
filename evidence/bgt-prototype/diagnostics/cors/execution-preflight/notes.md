# BGT execution preflight

Captured on 31 August 2026 for browser origin `http://localhost:5173`.

Browser verdict: **allowed for this origin**.

The response allows the requested origin, the POST method, and both requested
headers: `content-type` and `prefer`. A different browser origin still requires
the provider to allow that origin.
